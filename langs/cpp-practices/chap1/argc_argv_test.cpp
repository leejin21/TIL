#include <iostream>

int main(int argc, char* argv[])
{
    int width, height;
    std::cin >> width >> height;

    std::cout << width << height << std::endl;

    // for (int i = 0; i<argc; ++i)
    //     std::cout << argv[i] << '\n';
    
    // return 0;
    
}